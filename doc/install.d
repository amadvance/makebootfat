Name
	makebootfat - makebootfat Installation

	As root or user type:

		:./configure
		:make

	and as root to install the programs and the documentation
	type:

		:make install

	The documentation is in the man pages:

		:man makebootfat

